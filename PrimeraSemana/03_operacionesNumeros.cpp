#include <iostream>
#include <algorithm>
using namespace std;
typedef int64_t ll;

int main(){
    ll a, b;
    ll c;
    cin>>a>>b;
    cout<<"suma: "<<(a+b)<<"\n";
    cout<<"resta: "<<(a-b)<<"\n";
    cout<<"multiplicacion: "<<(a*b)<<"\n";
    cout<<"divison: "<<(a/b)<<"\n";
    cout<<"residuo: "<<(a%b)<<"\n";
    return 0;
}